<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.anhquan.weatherforecast">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_weather_forecast"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_weather_forecast"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.WeatherForecast">

        <activity android:name=".ui.WeatherActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER"/>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
    </application>


</manifest>